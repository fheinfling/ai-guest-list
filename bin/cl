#!/usr/bin/env bash
# Supervised Claude launcher — auto-switches seats on usage limits and resumes your work.
# Installed to ~/.local/bin by `acctsw install`. The stock `claude` is never shadowed.
exec acctsw run claude "$@"
